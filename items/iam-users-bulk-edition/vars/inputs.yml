---
schema_version: 1
tenancy:

  # --- Tenancy Spec ---
  name: my-test-realm

  # --- User Spec ---
  users:
    - username: john.smith
      state: present
      email: John.Smith@example.com
      first_name: John 
      last_name: Smith
      enabled: true
      roles:
        - name: ewc-iam-user
        - name: ewc-jhub-training-f7983a-attendee

    - username: jane.doe
      state: present
      email: Jane.Doe@example.com
      roles:
        - name: ewc-iam-user
        - name: ewc-jhub-training-f7983a-attendee
        - name: ewc-jhub-training-d3d5d9-attendee

    - username: ada.jameson
      email: Ada.Jameson@example.com
      state: present
      enabled: false

    - username: carlos.perez
      email: Carlos.Perez@example.com
      state: absent
      deletion_protection: false

  # --- Defaults ---
  # NOTE: apply to every user unless overridden per-user within users section
  defaults:  
    deletion_protection: true # Safety switch; must be true for any `state: absent` to be enforced.
    enabled: true
    email_verified: true
    required_actions:
      - UPDATE_PASSWORD
    roles_mode: merge
